# Claude8Code Environment Variables
# ===================================
# Copy this file to .env and fill in values.
# .env is gitignored - never commit secrets!
#
# Non-secret settings go in settings/settings.toml

# Optional API key for authentication
# When set, requests require this key in x-api-key or Authorization header
# Leave empty for open access (no authentication required)
CLAUDE8CODE_AUTH_KEY=

# Claude Code OAuth token (alternative to ~/.claude/credentials.json)
# Usually not needed - claude8code uses Claude CLI credentials automatically
# CLAUDE_CODE_OAUTH_TOKEN=
