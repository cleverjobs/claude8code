name: Docker Build & Push

on:
  # Run after Tests workflow completes on main
  workflow_run:
    workflows: ["Tests"]
    types: [completed]
    branches: [main]
  # Build (but don't push) on PRs for validation
  # Note: Tags are handled by release.yml which has its own docker job
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    # Only run if Tests workflow succeeded (or if triggered by PR)
    if: |
      github.event_name == 'pull_request' ||
      (github.event_name == 'workflow_run' && github.event.workflow_run.conclusion == 'success')
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # For workflow_run, checkout the commit that triggered the Tests workflow
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: krisjobs/claude8code
          tags: |
            type=raw,value=latest,enable=${{ github.event_name == 'workflow_run' }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
